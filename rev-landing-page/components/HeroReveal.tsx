import React from "react";

/**
 * HeroReveal — passthrough wrapper (blueprint animation removed).
 * The Magnetic Floating Depth system now lives entirely in Hero.tsx.
 */
export default function HeroReveal({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
